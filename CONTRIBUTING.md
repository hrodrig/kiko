# Contributing

PRs against `develop`. Releases from `main`.

1. `make release-check` must pass locally before tagging
2. Follow existing patterns (gghstats, kzero, groot)
3. Keep gocyclo ≤ 14, coverage ≥ 80%
4. Sign commits
